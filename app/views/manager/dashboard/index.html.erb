<h1>Сводная информация</h1>
<div class="row">
	<div class="col-md-4">
		<div class="alert alert-dismissable alert-info">
		<h4>Общая статистика:</h4>
			<ul class="list-unstyled">
				<li>Всегов избирателей: <%= @voters %></li>
				<li>Всего волонтеров: <%= @volunteers %></li>
				<li>Всего диалогов: <%= @conversations %></li>
			</ul>
		</div>
	</div>
	<div class="col-md-4">
		<div class="alert alert-dismissable alert-info">
		<h4>Статусы диалогов:</h4>
			<ul class="list-unstyled">
				<% @conversations_by_status.each do |conversation_group| %>
					<li><%= conversation_group.status_id.present? ? ConversationStatus.find_by_id(conversation_group.status_id).name : 'Без статуса' %>: <strong><%= conversation_group.total %></strong></li>
				<% end %>
			</ul>
		</div>
	</div>

	
	<div class="col-md-7">

	</div>
</div>

